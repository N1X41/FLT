class Rule {
  /// Переменная переписывания
  String variable;

  /// Правило переписывания
  String rule;

  // Конструктор класса Rule
  Rule({
    required this.variable,
    required this.rule,
  });
}
